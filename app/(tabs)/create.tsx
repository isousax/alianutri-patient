// Pseudo-tela do botão central "+" do tab bar.
// Nunca é exibida: o tabBarButton intercepta o toque e abre o menu de criação
// (ver CreateFab em (tabs)/_layout.tsx), em vez de navegar até aqui.
export default function CreatePlaceholder() {
  return null
}
